# build

## Description
Build the Slidev presentation for production output.

## Usage
Run: `/build`

## Steps
1. Verify `package.json` includes the `build` script.
2. Run `npm run build`.
3. Confirm output is generated in `dist/`.

## Expected Outcome
The production build completes successfully and `dist/` contains the built site.

