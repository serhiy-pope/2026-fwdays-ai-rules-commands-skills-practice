# export

## Description
Export slides to shareable formats (PDF/PPTX/PNG) using Slidev.

## Usage
Run: `/export`

## Steps
1. Verify `package.json` includes the `export` script.
2. Run `npm run export`.
3. Note the output files created by Slidev (format depends on config).

## Expected Outcome
Exported slide files are generated successfully.

